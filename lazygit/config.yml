gui:
  showIcons: true

keybinding:
  universal:
    undo: '<C-Z>'
    redo: 'z'
    submitEditorText: '<A-Enter>'
    appendNewline: '<Enter>'
  files:
    commitChanges: 'C'
    commitChangesWithEditor: 'c'

#git config --global core.editor "nvim"
